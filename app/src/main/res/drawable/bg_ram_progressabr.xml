<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!--定义seekbar滑动条的底色-->
    <item android:id="@android:id/background" android:drawable="@drawable/bg_ram_progress_track"/>

    <!--定义seekbar滑动条进度颜色-->
    <item android:id="@android:id/progress" >
        <scale
            android:drawable="@drawable/bg_ram_progress_tracktop"
            android:scaleWidth="100%" />
    </item>
</layer-list>